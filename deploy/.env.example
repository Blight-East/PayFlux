# PayFlux Environment Configuration
# Copy this file to .env and update values before running docker compose

# Required: Your API key for authenticating ingest requests
PAYFLUX_API_KEY=your-secret-api-key

# Required: Stripe secret key (test mode is fine for development)
STRIPE_API_KEY=sk_test_your_stripe_key

# Optional: Tier configuration
# tier1 = detection only (default)
# tier2 = adds processor_playbook_context + risk_trajectory
PAYFLUX_TIER=tier1
